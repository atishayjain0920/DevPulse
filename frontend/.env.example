# Base URL of the DevPulse Express API (no trailing slash).
# The frontend calls this with credentials: 'include' for httpOnly session cookies.
VITE_API_BASE_URL=http://localhost:4000

# Hosted Lovable Preview cannot reach localhost — point this at a publicly
# reachable HTTPS URL of the Express API to test real GitHub OAuth there.
